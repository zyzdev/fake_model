name: fake_model
description: Automatically generate code for create fake model by annotating Dart classes.
repository: https://github.com/zyzdev/fake_model
version: 0.0.3

environment:
  sdk: '>=2.19.3 <4.0.0'

topics:
  - data-model
  - build-runner
  - codegen

dependencies:
  meta: ^1.3.0
  analyzer: ^5.13.0
  build: ^2.0.0
  dart_style: ^2.3.1

dev_dependencies:
  lints: ^2.0.0
  build_runner: ^2.1.0